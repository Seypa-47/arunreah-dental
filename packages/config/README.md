# Shared configuration

This directory is reserved for shared tooling presets when they are needed by
more than one workspace package. Root ESLint and Prettier configuration is the
source of truth today; do not add duplicate configuration here without a clear
cross-package need.
