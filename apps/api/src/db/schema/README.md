# Drizzle schema modules

Add one focused schema module per future domain and export it through a small
schema index when tables are introduced. Do not add application tables until a
reviewed product requirement exists.
